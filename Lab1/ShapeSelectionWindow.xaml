<Window x:Class="Lab1.ShapeSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab1"
        mc:Ignorable="d"
        Title="Вибір фігури" Height="200" Width="300">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Виберіть фігуру:" FontSize="16" Margin="0,0,0,10" />

            <Button Name="rectangleButton" Margin="0,5" Click="RectangleButton_Click" Background="Transparent" BorderThickness="0">
                <StackPanel Orientation="Horizontal">
                    <Image Source="images/Прямокутник.png" Height="30" Width="30" Margin="0,0,5,0"/>
                   
                </StackPanel>
            </Button>

            <Button Name="circleButton" Margin="0,5" Click="CircleButton_Click" Background="Transparent" BorderThickness="0">
                <StackPanel Orientation="Horizontal">
                    <Image Source="images/Коло.png" Height="30" Width="30" Margin="0,0,5,0"/>
                   
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
